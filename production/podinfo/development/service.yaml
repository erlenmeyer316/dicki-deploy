apiVersion: v1
kind: Service
metadata:
  name: podinfo
spec:
  type: ClusterIP
  selector:
    app: podinfo
  ports:
    - name: http
      port: 7676
      protocol: TCP
      targetPort: http
    - port: 7777
      targetPort: grpc
      protocol: TCP
      name: grpc