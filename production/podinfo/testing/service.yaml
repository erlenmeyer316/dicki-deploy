apiVersion: v1
kind: Service
metadata:
  name: podinfo
spec:
  type: ClusterIP
  selector:
    app: podinfo
  ports:
    - name: http
      port: 8787
      protocol: TCP
      targetPort: http
    - port: 8888
      targetPort: grpc
      protocol: TCP
      name: grpc